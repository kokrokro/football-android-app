<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    android:minHeight="?attr/actionBarSize"
    app:titleTextColor="@color/colorBottomNavigationUnChecked"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    android:background="@color/colorWhite">
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical|start">
        <TextView
            android:id="@+id/categoryType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/manrope_regular"
            android:textColor="@color/colorBottomNavigationUnChecked"
            android:textSize="16sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical|end">
        <ImageButton
            android:id="@+id/setSingleReferee"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="@android:color/transparent"
            android:scaleType="centerCrop"
            android:layout_marginRight="20dp"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_referee"
            />
        <ImageButton
            android:id="@+id/cancelSetSingleReferee"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="@android:color/transparent"
            android:scaleType="centerCrop"
            android:layout_marginRight="20dp"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_close8"
            />
        <ImageButton
            android:id="@+id/saveSingleReferee"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="@android:color/transparent"
            android:scaleType="centerCrop"
            android:layout_marginRight="20dp"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_tick"
            />
    <ImageButton
        android:id="@+id/drawerBtn"
        android:layout_gravity="end"
        android:layout_marginEnd="20dp"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:scaleType="centerCrop"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_menu4"/>
    </LinearLayout>
    <!--<View-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_alignParentBottom="true"-->
        <!--android:layout_below="@+id/drawerBtn"-->
        <!--android:layout_marginTop="5dp"-->
        <!--android:layout_height="1dp"-->
        <!--android:background="@color/colorLightGray"/>-->
</LinearLayout>
</androidx.appcompat.widget.Toolbar>